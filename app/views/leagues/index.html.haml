.row
  .span6
    %h3 League Listing

    %table.table.table-striped
      %thead
        %tr
          %th League Name
          %th League Fee
      %tbody
        = render(@leagues) || "No Leagues Available"

    %br
    %a.btn.btn-primary{:href => new_league_path} Create New League
