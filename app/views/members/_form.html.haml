= simple_form_for [@league, @member], :html => { :class => 'form-inline'}, remote: true do |f|
  = f.input :name, :placeholder => "Full Name"
  = f.input :email, :placeholder => "Email Address"
  = f.input :phone, :placeholder => "Mobile Phone"
  = f.submit :submit, :class => 'btn'

